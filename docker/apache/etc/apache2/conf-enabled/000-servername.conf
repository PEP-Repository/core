# It is recommended that this file is overwritten in every configuration, and contains at least the ServerName entry.

# See  https://httpd.apache.org/docs/2.4/mod/core.html#servername
#ServerName example.nl

# If the name and/or port of the docker container of the authserver is different from the default value of `pep_authserver:8080`, you can overwrite it here.
#Define AUTHSERVER_LOCATION "authserver-example:8080"
