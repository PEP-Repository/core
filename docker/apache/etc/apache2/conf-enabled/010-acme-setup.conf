# setup for use of ACME. Actual domains to use acme for are in 002-acme.conf, so they can separately be overridden in project repo's

<IfDefine !NO_ACME>
  MDNotifyCmd /usr/local/bin/md-notify.sh
</IfDefine>